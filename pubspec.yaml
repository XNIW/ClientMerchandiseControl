name: client_merchandise_control
description: "Fondazione Flutter Android/iOS per il client Storefront Merchandise Control."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  app_links: ^7.2.1
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^17.3.0
  http: ^1.6.0
  intl: ^0.20.2
  shared_preferences: ^2.5.5
  supabase_flutter: ^2.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
